## 1. 取得輸入檔案修改日期

- [x] 1.1 在 `main()` 函式中，於開啟輸出檔案之前，使用 `os.path.getmtime()` 取得三個輸入檔案（`EXCEL_PATH`、`TXT_4GL_4FD`、`TXT_RPT_XML`）的最後修改時間，並格式化為 `YYYY-MM-DD HH:MM:SS`

## 2. 寫入輸入資料來源段落

- [x] 2.1 在輸出檔案的標題區塊（第 159-160 行）之後，新增「輸入資料來源」段落，列出三個輸入檔案的檔名與最後修改日期
- [x] 2.2 使用分隔線與格式讓輸出段落清楚易讀

## 3. 驗證

- [x] 3.1 執行 `chklog.py` 並檢查輸出報告開頭是否正確顯示輸入檔案資訊
